pre-commit:
  parallel: true
  jobs:
    - name: lint
      glob: "*.{js,ts,jsx,tsx,json,css}"
      run: npx biome check --no-errors-on-unmatched {staged_files}

    - name: typecheck
      run: npx tsc --noEmit
